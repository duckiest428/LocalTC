// The cards' data model is the site's own (site/cardmodel.js): the same file the dashboard and the app
// preview a card with, so what the pilot saw before sharing is what the public page shows.
export { MAX_QUOTE, MOMENT_LABELS, airportNames, distanceFraming, duration, flightCard, hoursFraming, moments } from "../../site/cardmodel.js";
